<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new log entry..." %>
  </div>
  <%= f.submit "Save Log", class: "btn btn-primary" %>
<% end %>
